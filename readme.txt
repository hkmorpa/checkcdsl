install python
       Please note to check add Python to PATH variable while installing the python

On command prompt run below commads to use pip
use 
python -m pip install --upgrade pip 

then on command prompt run
pip install selenium

pip install gspread
python.exe -m pip install --upgrade pip
pip install oauth2client
